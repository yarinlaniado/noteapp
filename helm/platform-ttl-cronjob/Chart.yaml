apiVersion: v2
name: platform-ttl-cronjob
description: Removes expired gitops/envs/* directories from git — ArgoCD's own prune+selfHeal does the actual namespace deletion
type: application
version: 1.0.0
appVersion: "1.0.0"
